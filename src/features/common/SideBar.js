import React, { Component } from 'react';

export default class SideBar extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="common-side-bar">
        Component content: common/SideBar
      </div>
    );
  }
}
