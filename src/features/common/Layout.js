import React, { Component } from 'react';

export default class Layout extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="common-layout">
        Component content: common/Layout
      </div>
    );
  }
}
